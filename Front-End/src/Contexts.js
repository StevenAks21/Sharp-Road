import { createContext } from "react";

// Use as insideBuilding setInsideBuilding, possible values: true, false, null
export const insideContext = createContext();

// use as language setLanguage, possible values: "English", "Indonesian"
export const languageContext = createContext();